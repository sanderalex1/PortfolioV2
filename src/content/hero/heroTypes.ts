export type Hero = {
  firstName: string;
  lastName: string;
  intro: string;
  primaryCta: { label: string; href: string };
  secondaryCta: { label: string; href: string };
  availability: string;
};
